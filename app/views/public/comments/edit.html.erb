<h4 class="text-center">口コミ編集</h4>
<div class = "container">
<%= form_with model: @comment, url:spot_comment_path(@comment.id), method: :patch do |f| %>

  <div class="form-group row">
    <%= f.label :rate,'評価 ', class:'col-md-4 col-form-label' %>
    <span id= "star"></span>
  </div>
    <div class="form-group row">
    <%= f.label :review, '口コミ内容 ', class:'col-md-4 col-form-label' %>
    <div class="col-md-9">
      <%= f.text_area :review, class: "form-control", rows: "8" %>
    </div>
  </div>



  <script>
  $(document).on('turbolinks:load', function() {
        let elem = document.querySelector('#star');
        if (elem == null) return;

        elem.innerHTML = ""
        let opt = {
          starOn: "<%= asset_path('star-on.png') %>",
          starOff: "<%= asset_path('star-off.png') %>",
          scoreName: 'comment[rate]',
          score: <%= @comment.rate %>,
        };
        raty(elem, opt);
      });
  </script>




  <div class="form-group row justify-content-end">
    <div class="col-md-9">
      <%= f.submit '投稿する', class:"btn btn-success btn-lg" %>
    </div>
  </div>

<% end %>
</div>
</div>
